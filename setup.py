from setuptools import setup, find_packages

setup(
    author = "Namshima Iordye",
    description = "Reena - Resume Analyzer and Feedback Generator.",
    version = "0.1.0",
    packages = find_packages(include=['reeana', 'reeana.*'])
)